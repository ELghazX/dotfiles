# dotfiles and manual setup

1. Install no Desktop EndeavourOS
2. Install hyprland and ML4W config files
3. for NVIDIA remove nvidia driver, and change driver with
   ```sudo pacman -S nvidia-inst nvidia-prime run```
   ```nvidia-inst -o```

